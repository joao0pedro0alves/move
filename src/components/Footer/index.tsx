import {Container} from "@mui/material"
import {StyledFooter} from "./styles"

function Footer() {
    return (
        <StyledFooter>
            <Container></Container>
        </StyledFooter>
    )
}

export default Footer
